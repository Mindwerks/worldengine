language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: 
  - CFLAGS='-std=c99' CXXFLAGS='-std=c++0x' pip install --allow-all-external -r requirements.txt
# # command to run tests
script: nosetests
